

const Header = ({title}) => {
    return (
        <div>
            <header>
                <h1 className="header" >
                    {title}
                </h1>
            </header>
        </div>
    )
}

export default Header
